import XCTest
@testable import MySwiftPackageManager

final class MySwiftPackageManagerTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(MySwiftPackageManager().text, "Hello, World!")
    }
}
